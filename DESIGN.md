# DESIGN — SLAM: Explore the Unknown World

เอกสารนี้สรุปการตัดสินใจออกแบบทั้งหมดของระบบ (เขียนใหม่ทั้งหมด ไม่ใช้โค้ดจาก repo เก่า)
ถ้าโค้ดกับเอกสารนี้ขัดกัน ให้ถือว่าเอกสารผิดและแก้ให้ตรงกับโค้ด หรือคุยกันก่อนเปลี่ยนโค้ด

---

## 1. สนามและสมมติฐาน

| หัวข้อ | ค่า |
|---|---|
| สนาม | เขาวงกตแบบ grid ช่องละ **0.60 × 0.60 ม.** กำแพงอยู่บน**เส้นแบ่งช่อง** |
| ขนาดสนาม | **ไม่รู้ล่วงหน้า** — แผนที่ต้องขยายเองตามที่สำรวจเจอ |
| จุดเริ่ม | หุ่นวางกลางช่องใดก็ได้ หันไปทางใดก็ได้ (ขนานกับแนว grid) |
| ขอบสนาม | ถือเป็นกำแพงธรรมดาที่ต้อง**วัดเจอจริง** ไม่มีการสมมติ boundary |
| Ground Truth | แบบ A: ระบุกำแพงรอบแต่ละช่อง (ใช้เฉพาะตอนให้คะแนน หุ่นไม่เห็น GT เลย) |

## 2. Sensor และหน้าที่

| Sensor | ติดตั้ง | หน้าที่หลัก | หน้าที่รอง |
|---|---|---|---|
| **ToF** (RoboMaster) | บน gimbal | หยุดกลางช่องแล้วหันวัด **4 ทิศแบบนิ่ง** (median หลายค่า) → จัดประเภทกำแพง | measurement ของ EKF, หยุดรถเมื่อเจอกำแพงหน้า, หยุดฉุกเฉิน |
| **Sharp IR** (analog, ผ่าน sensor adapter) | ซ้าย 1 ขวา 1 | ประคองให้วิ่งกลางทาง, measurement ของ EKF (lateral + heading) | ตรวจกำแพงซ้าย/ขวาสำรอง (โหวตร่วมกับ ToF) |
| **IR** (digital เจอ/ไม่เจอ) | หน้าซ้าย 1 หน้าขวา 1 (เอียง 45°) | ตรวจมุมอับที่ ToF/Sharp มองไม่เห็น → side step หลบแล้ววิ่งต่อ | ไม่เข้า EKF และไม่ยกเลิกการวิ่ง |
| **IMU (yaw)** | ในตัวหุ่น | เลี้ยว 90° แบบ closed loop, heading hold | prediction ของ EKF |
| **Wheel odometry** | ในตัวหุ่น | ประมาณระยะที่วิ่ง | prediction ของ EKF |

> ตำแหน่งติดตั้งจริง (offset/มุม) อยู่ใน `slam/config.py` → ค่าปัจจุบันเป็นค่าประมาณ **ต้องวัดบนหุ่นจริง**

## 3. ข้อตกลงเรื่องพิกัด (สำคัญที่สุด — ทุกโมดูลต้องใช้ตรงกัน)

```
          +y = N (ทิศที่หุ่นหันตอนเริ่ม)
            ▲
            │
   W ◄──── (0,0) ────► +x = E (ขวามือตอนเริ่ม)
            │
            ▼ S
```

- **Map frame:** จุดกำเนิด = กลางช่องเริ่มต้น, `+y` = ทิศที่หุ่นหันตอนเริ่ม (เรียกว่า N), `+x` = ขวามือ (E) หน่วยเมตร
- **Heading θ:** องศา แบบ**เข็มทิศ** — `0° = N`, **ตามเข็มนาฬิกาเป็นบวก** (`90° = E`), wrap อยู่ใน `[-180, 180)`
  เวกเตอร์ทิศ = `(sin θ, cos θ)`
- **Cell:** `(cx, cy)` เป็นจำนวนเต็ม ติดลบได้ ช่องเริ่ม = `(0, 0)` กลางช่อง = `(cx·0.6, cy·0.6)`
- **Direction:** `N=0, E=1, S=2, W=3` (ตามเข็มนาฬิกา) → heading ของทิศ `d` = `90·d`
- **Body frame:** `forward`, `right` และมุม sensor วัดจากด้านหน้า ตามเข็มนาฬิกาเป็นบวก
- **Robot API** (`slam/robot_api.py`) ส่งค่าทุกอย่างในข้อตกลงนี้แล้ว — การแปลงเครื่องหมาย/แกนของ RoboMaster SDK
  ทำใน `robot_io.py` ที่เดียว

## 4. แผนที่ (`slam/maze_map.py`)

- เก็บแบบ **sparse** (`dict`) ขยายได้ทุกทิศ ไม่จอง array
- กำแพงเก็บ**ที่ขอบ** ขอบละ 1 key: `(cx, cy, N)` หรือ `(cx, cy, E)` — ขอบ S/W แปลงเป็น N/E ของช่องข้างเคียง
  → สองช่องที่ติดกันเห็นกำแพงเส้นเดียวกันเสมอ ข้อมูลขัดกันเองไม่ได้
- แต่ละขอบมี **คะแนนหลักฐาน** (log-odds): `+w` เมื่อ sensor บอกว่าเป็นกำแพง, `−w` เมื่อบอกว่าเปิด
  - `score ≥ +threshold` → `WALL`, `score ≤ −threshold` → `OPEN`, ระหว่างนั้น → `UNKNOWN`
  - ขอบที่หุ่น**วิ่งผ่านจริง**แล้ว → ล็อกเป็น `OPEN` ถาวร
  - คะแนนมีเพดาน เพื่อให้หลักฐานใหม่กลับผลได้ถ้าอ่านผิดซ้ำๆ ก่อนหน้า
- ขนาดสนามที่หุ่นประมาณได้ = bounding box ของช่องที่รู้จัก

### รูปแบบ ASCII (ใช้ทั้งแผนที่ของหุ่นและไฟล์ GT)

```
+---+---+---+
| S     |   |      "+" = มุม, "---"/"|" = กำแพง, "   "/" " = เปิด, " ? "/"?" = ไม่รู้
+   +---+   +      อักษรกลางช่อง (S = จุดเริ่ม, E = จุดจบ, . = เคยไป) ไม่มีผลต่อกำแพง
|           |
+---+---+---+
```

แถวบนสุดของไฟล์ = `y` มากที่สุด คอลัมน์ซ้ายสุด = `x` น้อยที่สุด

## 5. Localization — EKF (x, y, θ)

- **State:** `[x, y, θ]` ใน map frame + covariance 3×3
- **Predict:** ใช้ส่วนต่างของ odometry + IMU yaw ในแต่ละรอบ
  - process noise เป็นแบบ **random walk**: *variance* ∝ ระยะที่วิ่ง / มุมที่หมุน / เวลา
    (ไม่ใช่ std ∝ ระยะต่อรอบ — แบบนั้นยิ่งรอบถี่ filter ยิ่งมั่นใจเกินจริง แล้ว gate จะปัดค่าที่ถูกทิ้งหมดจน filter หลง
    เจอจริงใน sim และมี regression test แล้ว)
- **Measurement (ทุกตัวคือ "ระยะถึงกำแพงที่รู้ตำแหน่ง")** — กำแพงอยู่ได้เฉพาะบนเส้น `±0.3 + k·0.6` เท่านั้น
  จึงคาดระยะที่ควรอ่านได้จาก state + แผนที่ แล้วใช้ส่วนต่างแก้ state
  - ToF 4 ทิศตอนหยุดกลางช่อง → แก้ x, y (และ θ อ่อนๆ)
  - Sharp ซ้าย/ขวาระหว่างวิ่ง → แก้ lateral และ θ (อัตราเปลี่ยนของระยะ)
  - ToF หน้าขณะเข้าใกล้กำแพงหน้า → แก้ระยะตามแนววิ่ง
- **Gate 2 ชั้น:**
  - Mahalanobis > 3σ → ทิ้ง (เช่น แสงลอดช่องทางเปิดไปเจอกำแพงไกล)
  - |innovation| > **0.15 ม. (¼ ช่อง)** → ทิ้งเสมอ เพราะกำแพงซ้ำกันทุก 0.6 ม. ถ้าต่างมากกว่านี้อาจเป็นกำแพงของ*ช่องข้างเคียง*
    (เคยลองขยาย covariance เมื่อถูกปัดทิ้งติดกันหลายครั้ง ผลคือ EKF กระโดดข้ามช่อง จึงเลิกใช้)
- **กำแพงไกลของช่องใหม่ (tentative):** ขณะวิ่งเข้าช่องที่ยังไม่เคยสแกน กำแพงข้างหน้ายังเป็น `UNKNOWN`
  จึงลองใช้ ToF หน้ากับ "เส้นขอบไกลของช่องเป้าหมาย" ไปก่อน ถ้าไม่มีกำแพงจริง gate จะปัดทิ้งเอง
  → แก้ระยะตามแนววิ่งได้ก่อนถึงช่อง ไม่งั้นทางเดินยาวๆ ที่ล้อลื่นจะวิ่งเลยเป้า
- **Heading มาจาก IMU:** การแก้ด้วยระยะกำแพงปรับแค่ x, y ไม่ปรับ θ (`ekf.heading_update_gain = 0`)
  และจำกัดความไม่แน่นอนของ θ ไว้ที่ 2° (ไม่งั้นโตเรื่อยๆ จนตำแหน่งดูไม่แน่นอนเกินจริง)
  - รันจริงครั้งแรก: offset ของ sensor ผิด → การแก้จากกำแพงดึง heading ของ EKF ออกจาก IMU ไป 14–32° → หุ่นเบี้ยวตาม
- **IR digital:** ไม่เข้า EKF — ใช้เป็น event (เจอมุม/เสา) และกันชน
- **Logical cell:** `(round(x/0.6), round(y/0.6))` + ตรวจว่าตรงกับช่องที่ตั้งใจไป
- **Heading lock:** มุมเป้าหมายตอนเลี้ยวเป็นค่าตายตัว `0/90/180/270` (ไม่ใช่ "บวกเพิ่ม 90 จากตอนนี้")

## 6. Exploration — DFS / Trémaux

- ที่ช่องใหม่: หยุด → ToF 4 ทิศ → อัปเดตแผนที่ → เลือกทางเปิดที่นำไปช่องที่**ยังไม่เคยไป**
- ไม่มีทางใหม่ → **ย้อนกลับ** ตาม stack ของ DFS (Trémaux mark กันวนซ้ำเมื่อเขาวงกตมี loop)
- **ToF ไม่มีค่า:** หันดูซ้ำ ±10° ถ้ายังไม่มีค่า → ถือว่า**เปิด** (กำแพงในระยะ 1 ช่องให้ค่าเสมอ)
  - รันจริง 03:05: ทางเดินยาวข้างหน้าช่องเริ่มอ่านไม่ได้ → เคยนับเป็น "ไม่แน่ใจ" → หุ่นเลยไปทางเดียวที่เปิดคือข้างหลัง (ทางออกหลอก)
- **ลำดับการเลือกทาง:** ตรง → ขวา → ซ้าย → หลัง แต่ทิศที่ค่า ToF "ดูเหมือนทางออก" (ไม่มีค่า / ไกลเกิน 3 ม. / ไม่ลงเส้น grid)
  ไว้**ท้ายสุด** → สำรวจทางปกติให้หมดก่อน
- **ทางออกหลอก (ออกนอกสนาม):** หลังเข้าช่องใหม่ ถ้า
  1. ไม่มีด้านไหนเป็นกำแพงเลย และ
  2. ToF ≥ 2 ทิศ (ไม่นับทางที่มา; ≥ 1 ทิศถ้าเข้ามาทางที่ดูเหมือนทางออกอยู่แล้ว) อ่านได้ผิดปกติ
     (ในเขาวงกตผิวกำแพงอยู่ที่ ขอบช่อง + k×0.6 ม. เสมอ ของในห้องอยู่ระยะไหนก็ได้)
  3. หันดูซ้ำ ±20° ก็ยังไม่เจอกำแพง
  → ออกนอกสนามแล้ว: ถอยกลับตามทางเดิมจนถึงช่องที่มีกำแพง ตัดช่องนอกสนามออกจากแผนที่ ห้ามกลับไปอีก แล้ว**สำรวจต่อ**
- **จบภารกิจ** เมื่อสำรวจครบเท่านั้น (ไม่มีช่องที่ไปได้แต่ยังไม่เคยไป) — ทางออกหลอกไม่ทำให้จบ
  - sim (ห้องล้อมสนามระยะสุ่ม): เขาวงกตปิด 20/20 สมบูรณ์; มีช่องเปิด 18/20 ไม่เหลือช่องนอกสนาม
    (2 ครั้งที่พลาด: ผนังห้องบังเอิญอยู่บนเส้น grid ทุกด้าน → ToF แยกไม่ออก)
- **วิ่งไม่ผ่าน:** ถ้าหยุดก่อนครึ่งช่อง → นับเป็นหลักฐานว่ามีกำแพง แล้วถอยกลับกลางช่อง
  ถ้าหยุด*หลัง*ครึ่งช่อง → ตัวหุ่นข้ามขอบไปแล้ว ขอบนั้นต้องเปิด → ถือว่าถึงช่องเป้าหมาย (สแกนต่อไปจะแก้ตำแหน่งเอง)
- **Safety cap:** จำนวนช่อง, ความกว้างแผนที่, เวลาภารกิจ — ถ้าชนให้หยุดอย่างปลอดภัยและบันทึกเหตุผล

## 7. Motion control (state machine)

| Primitive | วิธี |
|---|---|
| เลี้ยว ±90° / 180° | closed loop ด้วย IMU ไปยังมุมตายตัว |
| เดินหน้า 1 ช่อง | `drive_speed` closed loop ~20 Hz: heading hold (IMU) + จัดกลางทาง (Sharp) + หยุดด้วย odometry หรือ ToF หน้า |
| หยุดฉุกเฉิน | ToF หน้าต่ำกว่าเกณฑ์ → หยุดทันที |
| IR หน้า 45° (มุมอับ) | **ไม่หยุดการวิ่ง**: ติดข้างเดียว → side step หนีฝั่งนั้น (ไม่เกิน 5 ซม. จากเส้นกลาง) แล้ววิ่งต่อ; ติดทั้งคู่ → ช้าลงแล้ววิ่งต่อ |

รันจริง 03:32: กำแพงหนาทำให้ IR ทั้งคู่เห็นกำแพงข้างตั้งแต่เริ่มวิ่ง → เดิมยกเลิกการวิ่ง + บันทึกว่าเป็นกำแพง →
สำรวจได้ 6/20 ช่อง; sim ที่ให้ IR เห็นไกล 0.15–0.50 ม.: 15/15 สมบูรณ์ทุกระยะ

## 8. โครงสร้างโค้ด

```
slam/
  config.py      # ค่าคงที่ทั้งหมด (dataclass) รวมไว้ที่เดียว
  geometry.py    # direction, มุม, cell ↔ metric, ray casting
  robot_api.py   # interface กลางที่ทั้ง SimRobot และ RealRobot ต้องทำตาม
  maze_map.py    # WallMap (แผนที่ fix) + MazeMap (แผนที่ของหุ่น มีคะแนนหลักฐาน) + ASCII/JSON
  sim.py         # สุ่มเขาวงกต + SimRobot (ฟิสิกส์ + noise + sensor)
  robot_io.py    # RealRobot: RobotAPI บน RoboMaster SDK (ที่เดียวที่รู้แกน/เครื่องหมายของ SDK)
  sharp.py       # ตาราง calibrate ADC → ระยะของ Sharp
  perception.py  # ค่าดิบ → WALL/OPEN/UNSURE + ผูกค่าที่อ่านได้กับกำแพงในแผนที่
  localizer.py   # EKF (x, y, θ)
  motion.py      # Navigator: เลี้ยว / วิ่ง 1 ช่อง + EKF ทุก tick
  explorer.py    # DFS/Trémaux mission loop
  logger.py      # Exploration Log (CSV) + trajectory + sensors + map + summary
tools/
  evaluate.py    # Map Accuracy / Coverage เทียบ GT
  run_sim.py     # รันภารกิจเต็มใน sim แล้วให้คะแนนทันที
  run_robot.py   # รันภารกิจบนหุ่นจริง (+ ให้คะแนนถ้าใส่ --gt)
  check_robot.py # ตรวจ stream ของ sensor และเครื่องหมายทุกแกนบนหุ่นจริง
  calibrate_sharp.py  # วัด ADC ที่ระยะต่างๆ → calibration/sharp_*.json
  calibrate_mounts.py # วัด offset ของ ToF / Sharp เทียบกำแพงจริง → calibration/mounts.json
  plot_run.py    # map.png, trajectory.png, comparison.png ของ run
tests/           # pytest — ทุกอย่างรันบน laptop ได้โดยไม่ต้องมีหุ่น
```

**หลักการ:** logic ทั้งหมด (แผนที่, EKF, explorer) คุยกับหุ่นผ่าน `RobotAPI` เท่านั้น
→ ทดสอบกับ `SimRobot` ได้ครบก่อนแตะหุ่นจริง

## 9. การวัดผล (`tools/evaluate.py`)

- **Map Accuracy** = ช่องที่ **กำแพงครบ 4 ด้านตรงกับ GT** ÷ ช่องทั้งหมดใน GT × 100 (ด้านที่ `UNKNOWN` = ผิด)
- **Coverage** = ช่องที่**รู้กำแพงครบ 4 ด้าน** ÷ ช่องทั้งหมดใน GT × 100
- ตัวเลขเสริม: accuracy/coverage **รายกำแพง**, confusion (กำแพงที่ทายว่าเปิด ฯลฯ), **phantom cells** (ช่องที่หุ่นคิดว่ามีแต่อยู่นอก GT)
- **การจัดแนว:** แผนที่ของหุ่นอยู่ใน map frame → ต้องรู้ช่องเริ่มใน GT + ทิศที่วางหุ่น (`--start`, `--heading`)
  หรือใช้ `--auto-align` ลองหมุน 4 ทิศ × เลื่อนทุกตำแหน่งแล้วเลือกแบบที่ตรงที่สุด (ใช้ตรวจทานค่าที่ป้อน)

ตัวอย่าง (3×2):

```
GT                     หุ่น
+---+---+---+          +---+---+---+
|       |   |          |       |   |
+   +---+   +          +   +   +   +      ← พลาดกำแพงใต้ช่องกลางบน
|           |          |           ?      ← ไม่รู้กำแพงขวาของช่องขวาล่าง
+---+---+---+          +---+---+---+
```

Map Accuracy = 3/6 = 50%, Coverage = 5/6 = 83.3%, Wall accuracy = 15/17 = 88.2%

## 10. วิธีรัน

```bash
python3 -m venv .venv                      # Python 3.8.10
.venv/bin/pip install -r requirements-dev.txt
.venv/bin/python -m pytest                 # test ทั้งหมด ไม่ต้องมีหุ่น
.venv/bin/python -m tools.run_sim --width 4 --height 5 --loops 2 --seed 1   # ภารกิจเต็มใน sim
.venv/bin/python -m tools.evaluate --gt ground_truth/example_3x2.txt --map <map.json|map.txt>
```

ผลลัพธ์ของแต่ละ run อยู่ที่ `runs/<ชื่อ run>/`:

| ไฟล์ | ตรงกับสิ่งที่ต้องส่ง (README) |
|---|---|
| `map.png`, `map.txt`, `map.json` | 1. Map |
| `exploration_log.csv`, `sensors.csv` | 2. Exploration Log (คำสั่ง, ตำแหน่ง, เวลา, ค่า sensor) |
| `trajectory.png`, `trajectory.csv`, `summary.json` (จุดเริ่ม/จุดจบ) | 3. Robot Trajectory |
| `comparison.png`, `metrics.json` (เมื่อมี GT) | 4. Map Accuracy / Coverage |

`run_sim` / `run_robot` วาดรูปให้อัตโนมัติตอนจบ วาดใหม่ได้ด้วย `python -m tools.plot_run runs/<run> [--gt ...]`

สีในรูป: เส้นทางใช้ categorical slot 1–3 ตามลำดับ (EKF = ฟ้า, odometry = ส้ม ประ, truth = เขียวอมฟ้า จุด)
ผ่าน validator ทุกข้อ ยกเว้น contrast ของสีที่ 3 → จึงมี legend และรูปแบบเส้นต่างกันเสมอ;
สถานะช่อง (ถูก/ผิด/ยังไม่ครบ) ใช้ status palette คู่กับสัญลักษณ์ ✓ ✗ ? ไม่ใช้สีอย่างเดียว

### ผลใน sim (เขาวงกต 4×5 สุ่ม, จุดเริ่ม/ทิศสุ่ม, 10 seed ต่อระดับ)

| Noise | แผนที่ 100% | error ตำแหน่ง EKF สูงสุด | ชน |
|---|---|---|---|
| default (ล้อลื่น ~2%, drift 0.02°/s) | 10/10 | 4.8 ซม. | 0 |
| 2× | 10/10 | 9.4 ซม. | 0 |
| 3× (ล้อลื่นได้ถึง ~20%) | 9/10 | — | ตัวที่พังเพราะลื่น 20% |

ความลื่นของล้อแบบคงที่ที่รับได้: **±10–12%** (8/8 ถึง 7/8 seed) ที่ ±15% เหลือ 5/8

## 11. เตรียมหุ่นจริง (ทำตามลำดับ)

1. **ต่อสาย** Sharp และ IR เข้า sensor adapter แล้วตั้ง `RobotIO.sharp_*` / `ir_front_*` ใน `slam/config.py`
   ให้ตรง (adapter id ที่ตั้งบนบอร์ด + port 1/2)
2. `python -m tools.check_robot adapter` — โบกมือหน้า sensor ทีละตัว โปรแกรมบอกว่า port ไหนค่าเปลี่ยน
   → ใส่ port นั้นใน config (ถ้า ADC ค้างแถว ~510 ไม่ว่าวางกำแพงห่างเท่าไร = port ผิดหรือ sensor ไม่มีไฟ)
3. `python -m tools.check_robot streams` — ทุก stream ต้อง `True` และค่าดูสมเหตุสมผล
   (เอามือบัง sensor แต่ละตัวแล้วดูค่าเปลี่ยน, ToF ต้องเป็นตัวที่ `tof_index` ชี้)
4. `python -m tools.check_robot signs` — ขยับหุ่นเล็กน้อยเพื่อเช็คเครื่องหมาย ถ้าไม่ผ่านให้กลับ flag ตามที่พิมพ์บอก แล้วรันใหม่
5. `python -m tools.calibrate_mounts` — วางหุ่นกลางช่องที่มีกำแพงซ้าย-ขวา → วัด offset "ที่ใช้จริง" ของ ToF และ Sharp
   เทียบกับกำแพงจริง (รวมความหนากำแพงไว้แล้ว) → `calibration/mounts.json` ซึ่ง run_robot / check_robot โหลดเอง
   - ค่าตั้งต้นในไฟล์มาจากช่องเริ่มของรันจริงครั้งแรก: ToF 0.182 ม., Sharp 0.1845 ม. ต่อข้าง
     (ตัวปล่อยแสง ToF อยู่ห่างจุดหมุน ~8 ซม. ส่วนที่เกินน่าจะเป็นความหนากำแพง)
6. `python -m tools.calibrate_sharp --side left` และ `--side right` → ได้ `calibration/sharp_*.json`
   (commit ไฟล์นี้ด้วย) เช็คผลด้วย `--live`
7. ซ้อมในเขาวงกตเล็ก: `python -m tools.run_robot --gt ground_truth/<สนามซ้อม>.txt`
8. **วันส่ง:** วาด GT พร้อมลูกศรที่ช่องเริ่ม → วางหุ่นกลางช่อง ขนานกำแพง → `python -m tools.run_robot --gt ...`

ข้อสังเกตจาก repo เก่า (หุ่นตัวเดียวกัน): gimbal มักหยุดขาด ±180° ไป 3–4° → ตอนสแกนจึงใช้มุม**จริง**จาก feedback
ไม่ใช่มุมที่สั่ง

## 12. สถานะ

| ส่วน | สถานะ |
|---|---|
| DESIGN.md, config, geometry, robot_api, maze_map, sim, evaluate + tests | ✅ |
| perception, localizer (EKF), motion, explorer, logger, run_sim + tests | ✅ |
| robot_io (RealRobot), check_robot, calibrate_sharp, run_robot + tests (fake SDK) | ✅ |
| ทดสอบบนหุ่นจริง: check → วัด mount → calibrate → ซ้อม | ⬜ |
| plot_run (map / trajectory / comparison PNG) + ต่อเข้ากับ run_sim, run_robot | ✅ |
